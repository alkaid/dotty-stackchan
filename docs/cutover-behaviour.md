# dotty-behaviour Cutover Archive

This file used to contain the 2026-05 dotty-behaviour migration runbook.
The live deployment path has since moved to the root `compose.yml`.

For current deployment, service topology, health checks, and maintenance
rules, use [deployment.md](./deployment.md).
